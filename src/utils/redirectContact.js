const redirectContact = (type) => {
    if (type === 'github') window.location.replace('https://github.com/GlukhoedovNikita')
    if (type === 'telegram') window.location.replace('https://t.me/dvun0g')
}

export default redirectContact
